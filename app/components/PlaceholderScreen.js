//used as a palceholder/fake screen in tab navigator to use as a placeholder for the home screen and use stack createPost instead
import { View } from "react-native";

const PlaceholderScreen = () => {
  return <View></View>;
};

export default PlaceholderScreen;
